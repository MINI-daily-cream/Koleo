////describe("AccountPage", () => {
////  beforeEach(() => {
////    cy.visit(Cypress.config().baseUrl + "/login");
////  });

////  it("should contain ticket field on account page", () => {
////    cy.get('button[type="submit"]').click();
////    cy.contains("Bilety").should("exist");
////  });
////});
